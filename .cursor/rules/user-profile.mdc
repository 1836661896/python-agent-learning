---
description: 用户的学习背景、特点与偏好；助手应在对话中持续完善本档案
alwaysApply: true
---

# 用户学习档案

> **使用说明**：助手应**自主判断**何时更新本文件，无需用户每次提醒。出现以下情况之一时，可考虑更新：
> - 用户明确说出新的**偏好或习惯**（如学习方式、节奏、表达偏好）；
> - **进度或环境变化**（如完成某阶段、换设备、前端项目启动）；
> - 用户**纠正**助手的做法（如「你不要直接改代码」）→ 将纠正内容沉淀到画像；
> - 多次出现同类**提问或关注点**（如爱问「等价吗」「和 xx 的区别」）→ 可归纳进「已接触的概念」或「教学建议」。
> - 助手更新了**根目录 `readme.md` 或 `docs/` 下规划类文档**（索引、目标、changelog、协作规则等）且可能影响**如何配合用户**时 → 检查本画像是否需补一句（例如文档分工、提交习惯、是否允许改代码的重申）。

---

## 语言与表达

- **对话语言**：统一使用**中文**；用户英文阅读有限，仅能看懂简单英文。
- **术语**：避免大段未解释的英文术语；若必须使用，先给简短中文解释并说明在项目中的作用。

---

## 编程与学习背景

- **编程经验**：有 **JavaScript/TypeScript** 背景，能通过类比理解 Python（如 interface ↔ Pydantic BaseModel、catch/throw ↔ except/raise）。
- **Python**：从零学起，结合本项目（基础 Agent → Web API 服务化 → 未来自动化）边做边学。
- **学习偏好**：
  - **项目驱动**：不喜欢纯练习、无实际应用的语法练习；希望知识点在完善项目的各步骤中**按需、分散**引入，不要某一天集中练完一整类方法。
  - **理解优先**：会追问「为什么」「等价吗」「怎么用」（如 `if not description` 与 `if description == ""`、`classmethod` 是什么、`raise` 是否专用词）。
  - **节奏自控**：会主动「暂停」「继续学习」；完成一步后会说「好了」「第一步好了」再接下一步。
  - **文档维护偏好**：希望助手在每次学习结束后自动决定并更新 **`readme.md`**（当前架构与模块实现度）与 **`docs/changelog.md`**（变更流水）；**`docs/project-goals.md`** 仅在目标变化时改。文档分工见 **`docs/documentation-index.md`**。不需要每次单独提醒或征求确认。

---

## 项目与进度（随学习更新）

- **项目性质**：学习项目；首要目标是**学会**，其次才是完成功能。
- **当前进度**：**`POST /chat/stream`**：**`chat`/`mcp`/`auto`**；**`preset=schedule`**（**`src/prompts/presets/`**）；**`route_auto`** 不写死 MCP 工具名；**`mcp`** 发 **`tool_call`→`tool_result`→`delta`**。以 **`readme.md` §3** 为准；**`docs/changelog.md`** 记流水。
- **近期目标（与 `readme.md` §7 同步）**：**mcp-server**（**`export_schedule_document`**、下线 **`get_schedule_planning_guide`**）；**前端** preset 入口与 **`tool_call`/`tool_result`**。**backend 本阶段告一段落**。  
- **mcp-server 文档/规则（2026-05-17）**：已与 backend 对齐 **`readme` §1–§9**、**`collaboration-and-coding-rules`**、**`.cursor/rules`**；进度见 **`../mcp-server/readme.md` §3**。
- **前端**：**React**（`myproject/frontend`），与后端 API 联调；进度见 **`frontend/readme.md`**。
- **多设备/新对话**：会换设备或新开 Cursor 窗口；依赖 **`readme.md`**、**`docs/documentation-index.md`**、**`study-plan`**、**`checklist`** 同步进度；希望助手在新对话时先读上述文档再继续。
- **文档体系（2026-05-09）**：用户要求区分 **readme（当前真相）** / **changelog（详细变更）** / **project-goals（目标、少改）** / **`.cursor/rules`（助手硬规则）** / **`docs/collaboration-and-coding-rules.md`（人类可读摘要）** / **本画像**；并要求助手在**修改规划类文档后及时更新对用户的理解**。

---

## 操作与代码修改

- **助手不直接改 `.py`**：任何 **`*.py`**（含测试）仅当用户在该轮消息正文里写出**一字不差**的 **`本次允许修改`** 六字时才可由助手直接改；其它情况只给步骤与片段说明，由用户本人改。
- **其它脚本与配置**（如 `.sh`、`requirements.txt`）：助手不直接改，只说明操作步骤。
- **用户会自己写代码**：实现与练习以用户动手为主，助手负责讲解、答疑和指引。

---

## 教学与互动建议

- **讲解**：默认面向零基础 Python 学习者，新概念要**逐项解释**（目的、含义、可选写法）；可结合 JS/TS 类比。
- **步骤**：每步只给一小块目标与操作，等用户确认后再给下一步；避免一次塞入整类方法或大段代码。
- **答疑**：用户会对单行或片段提问（如 `@api.py:21-30 这个 try 加的对吗`、`except: 和 except Exception: 区别`），需**直接、简洁**回答并顺带补充一点扩展（如「还可以用 logger.exception」）。
- **画像更新**：用户明确希望「随时完善个人印象画像」时，应更新本文件（如补充新发现的偏好、环境变化、当前阶段描述）。

---

## 已接触的概念（随学习增补）

- Python：变量与作用域、list/dict、控制流、函数与参数、字符串方法（split/join/strip/startswith）、enumerate、datetime、import 与模块、try/except、raise、logging、json、文件 with open、FastAPI、Pydantic（BaseModel、Field、field_validator）、classmethod、dataclass 提及；项目中已接触 **SQLAlchemy ORM**、**Alembic**、**pytest / TestClient**、**SSE（流式响应）**、**httpx**（调用 Ollama 等）。
- 与 JS/TS 的类比：interface ↔ BaseModel、throw ↔ raise、catch ↔ except、类型与运行时校验。

---

## 技术环境（若已知）

- 仓库根目录含 **`src/`**（`api.py`、`agent_service.py`、`routers/`、`schemas/`、`llm/`、`models/`、`services/` 等）、**`.venv`**、**根目录 `readme.md`**；具体 OS/Shell 以用户实际环境为准，换设备后可能变化。

---
description: Python 学习路线与项目推进计划（按难易排序，与 python-learning-checklist.mdc 对照）
alwaysApply: true
---

**文档关系**：本文件定义阶段顺序与每阶段功能；根目录 **`readme.md`** 记录**当前架构、功能模块实现度**与「下一次学习的起点」；**`docs/changelog.md`** 记录详细变更流水；**`docs/project-goals.md`** 记录中长期目标（少改）；**`docs/documentation-index.md`** 说明各文档分工。`python-learning-checklist.mdc` 记录各知识点的未学/进行中/已学状态。新对话时先读 readme + documentation-index + 本文件 + checklist，即可接上用户需求与当前进度。

## 总体学习目标

- 结合本项目的长期目标（高级 agent + 自动化 + 视觉识别），系统学习并掌握：
  - Python 语言基础与常用标准库；
  - 项目结构、虚拟环境与依赖管理；
  - 使用 Python 编写 Web 后端（如 FastAPI）；
  - 可扩展的 agent 流程（命令解析、工具调用、日志记录）；
  - 为自动化脚本、屏幕识别与前端集成打基础。

**学习方式**：以项目功能为主线，在实现 list/add、持久化、API、Agent 等过程中穿插知识点；知识点进度以 `.cursor/rules/python-learning-checklist.mdc` 为准，教到即更新状态。

**常用方法的安排原则**：清单中的常用方法（如字符串的 strip/replace、列表的 insert/pop、内置函数 enumerate/zip 等）**不需要在某一天或某一步一次性练完**。应在项目完善过程中的**各步骤里按需引入**——例如在 1a 用到 list.append 就讲 append，在 1c 做 time 或解析时再讲 strip、enumerate，在 2a 做持久化时讲 open/with。助手设计教学时按当前阶段功能需要挑选少量方法讲解即可，避免单次教学塞入整类方法。

---

## 按难易程度的项目推进顺序

整体顺序：**易 → 中 → 较难 → 难**。每步对应「项目要做的功能」和「建议在清单中标注的章节」。

| 顺序 | 阶段 | 难易 | 项目功能/目标 | 主要涉及清单章节 |
|------|------|------|----------------|------------------|
| 0 | 阶段 0：环境与骨架 | 易 | 环境、最小命令行、命令拆分、help/version/echo | §1 基础、§3 控制流、§4 函数、§5 字符串 |
| 1a | 阶段 1a：基础巩固 | 易 | **list / add 命令**（内存任务列表） | §1 list/dict、§4 函数参数与 docstring、§5.5 len/range |
| 1b | 阶段 1b：异常与健壮性 | 易 | **输入与解析处加 try/except**，友好错误提示 | §7 异常处理 |
| 1c | 阶段 1c：更多命令与内置 | 易~中 | **time 命令**、更多字符串（strip/replace）、类型转换、in / enumerate | §5 字符串、§5.5 内置函数、§2 运算符 in |
| 1d | 阶段 1d：模块与包 | 中 | 将命令处理拆到独立模块、**import 与包结构** | §6 模块与包 |
| 2a | 阶段 2a：文件与 IO | 中 | **任务持久化**（保存/读取到文件）、with、pathlib | §8 文件与 IO、§5.5 open |
| 2b | 阶段 2b：工程化 | 中 | **requirements.txt**、**logging**、可选 .env | §9 logging、§11 工程化 |
| 3 | 阶段 3：Web API | 较难 | **FastAPI**：GET /health、POST /tasks，逐步演进为后端服务核心 | §9 json、§10 FastAPI/requests |
| 4 | 阶段 4：Agent 工具系统 | 较难 | Task/Step 数据结构、工具封装、统一日志 | §4 进阶、§9 logging |
| 5 | 阶段 5：自动化与视觉 | 难 | 截屏、键鼠自动化、简单图像识别（预研） | §10 pyautogui、Pillow/OpenCV |
| 6 | 阶段 6：数据库与 ORM | 较难 | **PostgreSQL/MySQL**、**SQLAlchemy/SQLModel**、**Alembic 迁移**；任务与步骤落库，替代或并存 `tasks.json` | §8/§11、清单待扩：ORM、DB |
| 7 | 阶段 7：配置、容器与 CI | 中~较难 | **环境变量/.env**、**Docker/docker-compose**（后端+DB 一键起）、可选 **GitHub Actions** 跑测试/构建 | §11 工程化 |
| 8 | 阶段 8：测试与质量 | 中 | **pytest**、接口/业务测试、关键路径覆盖 | 清单待扩：pytest |
| 9 | 阶段 9：鉴权与安全 | 较难 | **JWT/会话**、受保护路由、密码哈希；API 与前端联调登录态 | §10 FastAPI 扩展 |
| 10 | 阶段 10：异步任务与缓存 | 难 | **Redis**（缓存/会话）、**后台任务**（Celery/RQ 或轻量队列）；长耗时 Agent 步骤异步化 | §9、清单待扩 |
| 11 | 阶段 11：可观测性与 API 规范 | 中~较难 | **结构化日志**、请求追踪；**分页/筛选/幂等/限流** 等真实接口习惯 | §9 logging |

**支线（按需插入，不单独占主阶段序号）**：

| 支线 | 内容 | 说明 |
|------|------|------|
| R1 | **运维/数据脚本** | 定时备份、迁移数据、CLI 小工具；巩固 `argparse`、异常处理 |
| R2 | **爬虫与采集** | `requests`、解析、合规与频率控制；与「数据采集类 Agent 工具」衔接 |
| R3 | **消息队列（进阶）** | RabbitMQ/Kafka 等：多 Worker、削峰；在 Agent 规模化时考虑 |

> **实施原则**：主路线仍按 0→5 与 6→11 顺序推进；**支线在需要时插入 1～2 次学习**，避免与主线抢进度。数据库（阶段 6）建议在 Agent 与 API 稳定后再上，迁移成本更低。

---

## 阶段 0：环境与项目骨架（已完成）

- **目标**：搭建环境，跑通最小可交互命令行，结构清晰便于扩展。
- **任务清单**：
  - ✅ Git 仓库、虚拟环境 `.venv`、`src/main.py`。
  - ✅ 最小命令行循环（欢迎语、多轮输入、`quit` 退出）。
  - ✅ 拆分为函数与配置（HELP_MESSAGE、SYSTEM_MESSAGE、COMMAND_HANDLERS）。
  - ✅ `help`、`version`、`echo` 命令。
- **对应清单**：§1 基础语法与数据类型、§3 控制流、§4 函数、§5 字符串。

---

## 阶段 1a：基础巩固（易）

- **目标**：在项目中巩固 list、函数参数、docstring；实现「内存任务列表」。
- **项目功能**：
  - 增加全局 `TASKS` 列表；实现 **list** 命令（展示任务，空时提示「暂无任务」）。
  - 实现 **add 任务内容** 命令（解析 `add xxx`，用 `TASKS.append(...)`）；在 HELP_MESSAGE 中登记 list/add。
- **知识点**：list 创建/append/遍历、函数参数与返回值、docstring；可选 `in`、`enumerate`。
- **对应清单**：§1 列表与字典、§4 函数（位置/默认参数、docstring）、§5.5 len/range。

---

## 阶段 1b：异常与健壮性（易）

- **目标**：掌握 try/except，提升程序健壮性。
- **项目功能**：
  - 在 `input()` 或命令解析处使用 **try/except**，对异常输入给出友好提示（不崩溃）。
  - 可选：对 `add` 的空内容做校验。
- **知识点**：try/except、捕获特定异常、可选 else/finally。
- **对应清单**：§7 异常处理。

---

## 阶段 1c：更多命令与内置（易~中）

- **目标**：熟悉更多字符串方法、内置函数、运算符。
- **项目功能**：
  - 增加 **time** 命令（用 `datetime` 或 `time` 显示当前时间）。
  - 在解析中用到 **strip**、**replace** 等；用 **in** 判断成员；用 **enumerate** 打印带序号列表。
- **知识点**：str.strip/replace、datetime、§5.5 enumerate/类型转换、§2 成员运算符 in。
- **对应清单**：§5 字符串、§5.5 内置函数、§9 datetime。

---

## 阶段 1d：模块与包（中）

- **目标**：理解 import 与包结构，代码分模块。
- **项目功能**：
  - 将命令处理逻辑拆到独立模块（如 `src/agent_service.py` 或 `src/agent/` 包），供 API 路由统一调用。
- **知识点**：import、from...import、__init__.py、相对导入。
- **对应清单**：§6 模块与包。

---

## 阶段 2a：文件与 IO（中）

- **目标**：掌握文件读写与 with、路径处理。
- **项目功能**：
  - **任务持久化**：启动时从文件读取任务列表，退出或 add/remove 时写回；使用 **with open**、可选 **pathlib**。
- **知识点**：open/read/write/close、with 上下文管理器、pathlib 或 os.path。
- **对应清单**：§8 文件与 IO、§5.5 open。

---

## 阶段 2b：项目工程化（中）

- **目标**：依赖声明、日志、简单配置。
- **项目功能**：
  - 创建 **requirements.txt**，加入当前依赖（若已用 FastAPI 则一并写入）。
  - 在项目中加入 **logging**，在关键流程打日志；可选 **python-dotenv** 读 .env。
- **知识点**：requirements.txt、logging、pip install。
- **对应清单**：§9 logging、§11 项目与工程化、§10 python-dotenv。

---

## 阶段 3：Python Web 后端与 API（较难）

- **目标**：用 FastAPI 提供 API，与命令行共用业务逻辑。
- **项目功能**：
  - FastAPI 应用：**GET /health**、**POST /tasks**（接收任务描述，返回任务 ID）；与现有命令行共用「任务」相关逻辑。
  - 使用 **json** 序列化请求/响应。
- **知识点**：FastAPI 路由、请求/响应模型、json、requests（若调用外部 API）。
- **对应清单**：§9 json、§10 FastAPI、requests。

---

## 阶段 4：Agent 流程与工具系统（较难）

- **目标**：可扩展的 Agent 内核，任务/步骤结构与工具封装。
- **项目功能**：
  - 定义 Task/Step 数据结构（类或 dataclass）。
  - 将 help/time/echo/list/add 等封装为「工具」，统一命令解析 → 工具选择 → 执行 → 日志。
- **对应清单**：§4 进阶、§9 logging。

---

## 阶段 5：自动化与视觉基础（难，预研）

- **目标**：为后续自动化、屏幕识别打基础。
- **项目功能**：
  - 截屏与简单图像处理（Pillow/OpenCV）；键鼠自动化（pyautogui）；在 Agent 中增加触发自动化的命令。
- **对应清单**：§10 pyautogui、Pillow、OpenCV。

---

## 阶段 6：数据库与 ORM（较难）— 接近真实持久化

- **目标**：用关系型数据库存任务（及后续步骤表），掌握 **ORM + 迁移** 的标准路径。
- **项目功能**：
  - 选型：**PostgreSQL** 或 **MySQL/MariaDB**（二选一；学习阶段可 Docker 本地起实例）。
  - 使用 **SQLAlchemy 2.x** 或 **SQLModel** 定义模型；用 **Alembic** 管理表结构变更。
  - 将 `GET/POST/DELETE /tasks` 与 Agent 读写逐步迁到数据库（可先双写或一次性迁移 `tasks.json`）。
- **对应清单**：在 `python-learning-checklist.mdc` 中逐步补充 DB/ORM 相关条目并标注已学。

---

## 阶段 7：配置、容器与 CI（中~较难）

- **目标**：接近「可部署」的工程形态。
- **项目功能**：
  - **配置与密钥**：数据库 URL、密钥等走环境变量与 `.env`（不入库、不提交仓库）。
  - **Docker**：`Dockerfile` + `docker-compose` 编排「API + 数据库」。
  - **可选 CI**：提交后自动跑 `pytest` 或镜像构建（如 GitHub Actions）。
- **对应清单**：§11 工程化；清单可扩 Docker/CI 备注。

---

## 阶段 8：测试与质量（中）

- **目标**：改动不破坏已有功能。
- **项目功能**：
  - **pytest** 覆盖核心业务与关键 API（健康检查、任务 CRUD、Agent 主路径）。
  - 可选：接口层集成测试（TestClient）。
- **对应清单**：pytest（清单中单独条目）。

---

## 阶段 9：鉴权与安全（较难）

- **目标**：接口具备真实系统的访问控制雏形。
- **项目功能**：
  - 用户注册/登录（简化即可）、**JWT** 或 Session、保护部分路由。
  - 密码存储：**哈希**（如 passlib/bcrypt），明文不落库。
- **对应清单**：FastAPI 安全相关；与前端联调携带 Token。

---

## 阶段 10：异步任务与缓存（难）

- **目标**：慢操作不阻塞 HTTP；热点数据可缓存。
- **项目功能**：
  - **Redis**：缓存、限流、会话存储（按需选）。
  - **后台任务**：长耗时步骤用队列（Celery/RQ 等）异步执行，结果轮询或 WebSocket（进阶）。
- **对应清单**：按需扩展第三方库条目。

---

## 阶段 11：可观测性与 API 规范（中~较难）

- **目标**：线上排障与协作效率。
- **项目功能**：
  - **结构化日志**、请求 ID、关键业务字段打点。
  - **API 习惯**：分页、筛选、错误码约定、幂等（如重复提交）、必要时限流。
- **对应清单**：§9 logging 深化。

---

## 当前进度与下一步（与 readme 同步）

- **已完成（以根目录 `readme.md` §3 功能模块表为准）**：阶段 0～8 关键能力已具备；数据库 + ORM + 迁移；**`/chat` / `/chat/stream`（SSE）**、planner、MCP、**会话记忆**、**`GET /events`**、**doc-sessions 支线**、Docker compose、多提供商 LLM 等 — **详细日期与条目见 `docs/changelog.md`**。
- **建议下一步**：见 **`readme.md` → §7 下一次学习的起点**（当前主线含：非流式 chat→MCP 补齐 **`conversation_id`/`turn_id`**、前端阶段 3+SSE、doc_sessions 与通用会话衔接等）。
- **提交前文档**：更新 **`readme.md`（§3、§7）** 与 **`docs/changelog.md`**；**`docs/project-goals.md`** 仅在产品目标变化时更新；产品边界见 **`readme.md` §3.1** 与 **`docs/project-goals.md`**。
- **新对话 / 换设备**：先读 **`readme.md`**、**`docs/documentation-index.md`**、本文件、**`python-learning-checklist.mdc`**、**`user-profile.mdc`**。

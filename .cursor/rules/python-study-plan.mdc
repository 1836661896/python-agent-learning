---
description: Python 学习路线与当前进度记录（backend 项目）
alwaysApply: true
---

## 总体学习目标

- 结合本项目的长期目标（高级 agent + 自动化 + 视觉识别），系统学习并掌握：
  - Python 语言基础与常用标准库；
  - Python 项目结构、虚拟环境与依赖管理；
  - 使用 Python 编写 Web 后端（例如基于 FastAPI 或 Flask）；
  - 设计并实现一个可扩展的 agent 流程（命令解析、工具调用、日志记录）；
  - 为后续的自动化脚本、屏幕识别和前端 React 集成打下基础。

## 阶段划分与任务清单

### 阶段 0：环境与项目骨架（当前）

- 目标：在本地搭建干净的 Python 项目环境，并跑通最小可交互的命令行程序，为后续 agent 打基础。
- 任务清单（✅ 已完成，⬜ 待完成）：
  - ✅ 本地 Git 仓库初始化，并与 GitHub / Gitee 远程仓库关联、推送首个提交。
  - ✅ 在 `backend` 目录中创建并激活虚拟环境 `.venv`（`python3 -m venv .venv` + `source .venv/bin/activate`）。
  - ✅ 创建 `src` 目录与 `src/main.py` 文件。
  - ✅ 实现一个最小的命令行循环程序：
    - 欢迎信息；
    - 多轮输入；
    - 固定退出命令 `quit`。
  - ⬜ 将当前命令行程序整理为“更清晰的结构”（拆分成函数，便于后续扩展为 agent 入口）。

### 阶段 1：Python 语言基础与语法巩固

- 目标：达到“能独立阅读和编写中小型 Python 模块”的程度。
- 主要内容：
  - 基础语法：变量、数据类型（str/int/list/dict 等）、if/while/for、函数定义与返回值。
  - 容器与常用操作：列表推导式、字典遍历、切片等。
  - 模块与包：`import`、相对导入、`__init__.py` 的作用。
  - 错误与异常处理：`try/except`、自定义异常。
- 任务清单（示例）：
  - ⬜ 在 `src` 中新建一个 `basics` 模块，用若干小练习巩固语法。
  - ⬜ 使用列表/字典实现一个小型“任务列表”结构（仅在内存中）。
  - ⬜ 编写几段包含异常处理的代码（例如解析用户输入时给出友好错误）。

### 阶段 2：项目结构与依赖管理

- 目标：掌握 Python 项目的基本工程化，包括依赖声明与简单配置。
- 主要内容：
  - `requirements.txt` / `pyproject.toml` 的作用与写法（先用简单版本）。
  - 基础日志记录（`logging` 模块）。
  - 简单配置管理（如读取 `.env` 或配置文件的方式——视进度选择）。
- 任务清单（示例）：
  - ⬜ 在项目根目录创建 `requirements.txt`，记录最初的依赖（例如 Web 框架）。
  - ⬜ 在 `src` 中加入简单的日志系统，并在命令行程序中打印结构化日志。

### 阶段 3：Python Web 后端与 API

- 目标：使用一个主流轻量框架（推荐 FastAPI）实现基础 API，为前端和 Agent 提供接口。
- 主要内容：
  - FastAPI 项目结构、路由与请求/响应模型。
  - 简单依赖注入、异常处理、中间件（按需）。
  - 与命令行入口的关系设计（例如共用同一套“任务执行逻辑”）。
- 任务清单（示例）：
  - ⬜ 创建基础 FastAPI 应用，提供 `GET /health` 健康检查接口。
  - ⬜ 提供一个 `POST /tasks` 接口，接收“任务描述”，返回任务 ID。
  - ⬜ 将当前命令行程序背后的逻辑重用到 API 层（避免两套重复逻辑）。

### 阶段 4：Agent 流程与工具系统

- 目标：搭建一个可以扩展的“Agent 内核”，支持多种工具调用和任务状态管理。
- 主要内容：
  - 任务与步骤的内部数据结构设计。
  - 简单的“命令解析 → 工具选择 → 执行 → 日志记录”流程。
  - 初步集成少量工具（例如：时间查询、文件读写、调用外部 API）。
- 任务清单（示例）：
  - ⬜ 定义 Task/Step 的 Python 数据结构（类或 dataclass）。
  - ⬜ 为命令行入口增加 `help` / `time` / `echo` 等基础命令。
  - ⬜ 将这些命令封装为工具（functions），为后续扩展做准备。

### 阶段 5：自动化与视觉能力的基础（预研阶段）

- 目标：为未来“操作游戏/软件、基于屏幕内容决策”的高级目标打基础。
- 主要内容（视进度逐步引入）：
  - 截屏与简单图像处理（如使用 Pillow/OpenCV）。
  - 键鼠自动化（如 `pyautogui` 或其他库）。
  - 将这些能力封装为 Agent 可调用的工具。
- 任务清单（示例）：
  - ⬜ 在桌面环境中实现一个“自动点击固定区域”的小脚本（非游戏场景）。
  - ⬜ 在命令行 Agent 中增加一个命令，用于触发上述自动化工具。

## 当前进度记录（简要）

- 环境：
  - ✅ Python 3.9.6 已确认。
  - ✅ 虚拟环境 `.venv` 已创建并能正常激活，`python` 指向 `.venv/bin/python`。
- 项目结构：
  - ✅ `src` 目录已创建。
  - ✅ `src/main.py` 已实现一个可多轮交互的命令行程序，支持 `quit` 退出。
- 下一步建议：
  - ⬜ 优化命令行程序结构，为后续命令扩展做准备（例如：增加 `help` 命令、拆分处理逻辑）。
  - ⬜ 同时开始系统讲解并练习 Python 的基础语法（结合当前 `main.py` 中的代码来解释）。

